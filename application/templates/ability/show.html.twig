{% extends 'base.html.twig' %}

{% block title %}Ability{% endblock %}

{% block body %}
    <h1>Ability</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ability.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ability.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ ability.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ability_index') }}">back to list</a>

    <a href="{{ path('ability_edit', {'id': ability.id}) }}">edit</a>

    {{ include('ability/_delete_form.html.twig') }}
{% endblock %}